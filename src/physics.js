// handles the game physics for the server

//const characters = {};

// move player simulation
const movePlayer = (data, otherPlayers) => {
  
}; 

const updateSimulation = () => {};

module.exports.updateSimulation = updateSimulation;
module.exports.movePlayer = movePlayer;
